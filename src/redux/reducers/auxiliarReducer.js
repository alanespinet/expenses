const defaultState = {
  places: [],
  types: [],
  users: []
}

const auxiliarReducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default auxiliarReducer
