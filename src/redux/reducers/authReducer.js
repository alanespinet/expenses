const defaultState = {
  authenticated: false,
  authenticatedUser: ''
}

const authReducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default authReducer
